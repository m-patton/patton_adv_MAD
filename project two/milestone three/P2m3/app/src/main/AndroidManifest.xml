<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atlas.p2m3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MindActivity"
            android:label="@string/title_activity_mind" />
        <activity android:name=".DataActivity" />
        <activity android:name=".MindTipsActivity" />
        <activity android:name=".BodyActivity" />
        <activity android:name=".BodyTipsActivity" />
        <activity android:name=".SpiritActivity" />
        <activity android:name=".SpiritTipsActivity"></activity>
    </application>

</manifest>